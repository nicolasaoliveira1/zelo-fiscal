# Dependencias de desenvolvimento e teste (instalar com: pip install -r requirements-dev.txt)
-r requirements.txt
pytest==9.0.3
ruff==0.15.16
# Timeout por teste: converte um hang (ex.: metadata lock do MySQL) em falha
# nomeada em vez de pendurar o CI ate o teto de 6h (spec 06 / job testes-mysql).
pytest-timeout==2.3.1
# Emite a falha de teste como annotation do GitHub Actions (file:line + mensagem),
# legivel pela API — util para diagnosticar falha que so acontece no CI.
pytest-github-actions-annotate-failures==0.3.0
# Paralelismo da suite (-n auto). A suite e quase toda CPU/setup, entao escala
# bem: ~1479 testes caem de ~152s para ~45s em 4 workers. Cada worker e um
# processo com o SEU banco (ver o sufixo por worker no conftest).
pytest-xdist==3.6.1
